import OwnReact from "../src";
import App from "./App";

const root = document.getElementById("root");

// eslint-disable-next-line react/no-deprecated, react/jsx-filename-extension
OwnReact.render(App, root);
